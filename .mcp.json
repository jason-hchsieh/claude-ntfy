{
  "mcpServers": {
    "claude-ntfy": {
      "command": "npx",
      "args": ["claude-ntfy"],
      "env": {
        "NTFY_TOPIC": "${NTFY_TOPIC}",
        "NTFY_SERVER_URL": "${NTFY_SERVER_URL}",
        "NTFY_TOKEN": "${NTFY_TOKEN}"
      }
    }
  }
}
